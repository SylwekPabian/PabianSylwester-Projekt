<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nieruchomosci">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".ViewPropertyActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ViewPropertyActivity" />
        </activity>
        <activity
            android:name=".AddPropertyActivity"
            android:parentActivityName=".ViewPropertyActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ViewPropertyActivity" />
        </activity>
        <activity
            android:name=".ViewPropertyDetailsActivity"
            android:parentActivityName=".ViewPropertyActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ViewPropertyActivity" />
        </activity>
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".ViewPropertyActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ViewPropertyActivity" />
        </activity>
        <activity android:name=".ViewPropertyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>